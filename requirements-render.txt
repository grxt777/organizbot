# Специальная версия requirements для Render.com
# Использует только pre-compiled wheels для избежания компиляции

--only-binary=all

aiogram==3.15.0
apscheduler==3.10.4
aiosqlite==0.20.0
python-dotenv==1.0.1
psutil==6.1.0

# Принудительно используем совместимые версии
pydantic>=2.6.0,<3.0.0
aiohttp>=3.9.0,<4.0.0 